<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.root.parliament">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".TimeLostActivity" />
        <activity android:name=".LSGovActivity" />
        <activity android:name=".RSGovActivity" />
        <activity android:name=".RSPvtActivity" />
        <activity android:name=".LSPvtActivity" />
        <activity android:name=".LSMemberActivity" />
        <activity android:name=".RSMemberActivity" />
        <activity android:name=".Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Admin"
            android:label="@string/title_activity_admin"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".success" />
        <activity android:name=".search" />
        <activity android:name=".result"></activity>
    </application>

</manifest>